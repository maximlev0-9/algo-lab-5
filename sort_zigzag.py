# def sort(arr: list):
#     for i in range(len(arr)-1):
#         for k in range(i+1, len(arr-1)):
#             if compare(arr, i, k):
#                 swap(arr, i, k)
#                 break

# def swap(arr: list, i:int, j:int ):
#     (arr[i], arr[j]) = (arr[j], arr[i])

# def compare(arr: list, i:int, j:int):
#     # if arr[i]==arr[j]:
#     #     return None

#     if i%2==0:
#         return arr[i] > arr[j]
#     else:
#         return arr[i] < arr[j]


# create hashmap
# add all values to it
# check for element with the biggest num for repeats
# add 